# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/service_container.html
parameters:
    #parameter_name: value

services:
#    app.doctrine_poster_listener:
#        class: Randomovies\EventListener\PosterUploadListener
#        arguments: ['%posters_directory%']
#        tags:
##            - { name: doctrine.event_listener, event: prePersist }
##            - { name: doctrine.event_listener, event: preUpdate }
#            - { name: doctrine.event_listener, event: postLoad }

    app.twig.random_movies_extension:
        class: Randomovies\Twig\Extension\RandomMoviesExtension
        arguments: ["@doctrine.orm.entity_manager"]
        tags:
            - { name: twig.extension }

    app.twig.total_users_notes:
        class: Randomovies\Twig\Extension\TotalUsersNotesExtension
        tags:
            - { name: twig.extension }

    app.randomovies_mailer:
        class: Randomovies\Service\Mailer
        arguments:
            - "@mailer"
            - "%mailer_sender%"
            - "@templating"

    app.extractor.movie:
        class: Randomovies\Extractor\MovieExtractor
        arguments:
            - "@validator"
