fos_elastica:
    clients:
#        default: { host: %elastic_host%, port: %elastic_port% }
#        default: { host: '%env(ELASTIC_HOST)%', port: '%env(ELASTIC_PORT)%' }
#        default: { host: 172.18.0.16, port: '%env(ELASTIC_PORT)%' }
        default: { url: '%env(ELASTIC_URL)%' }
#    serializer:
#        callback_class: FOS\ElasticaBundle\Serializer\Callback
#        serializer: serializer
    indexes:
        randomovies:
            client: default
            types:
                movie:
                    # config trouvée sur le site de l'afsy
                    mappings:
                        title: ~
                        synopsis: ~
                        genre: ~
                    persistence:
                        driver: orm
                        model: AppBundle\Entity\Movie
                        provider: ~
                        listener: ~
                        finder: ~
                        repository: AppBundle\SearchRepository\MovieRepository

                    mappings:
#                        id:
#                            type: integer
#                        title: { search_analyzer: custom_search_analyzer, index_analyzer: custom_index_analyzer, type: string }
#                        director: ~
#                        actors: ~
#                        year:
#                            type: integer
#                        duration:
#                            type: integer
#                        synopsis: ~
#                        rating:
#                            type: integer
#                        genre: ~
#                    persistence:
#                        driver: orm
#                        model: AppBundle\Entity\Movie
#                        finder: ~
#                        provider: ~
#                        listener: ~
#                        repository: AppBundle\Entity\MovieSearchRepository